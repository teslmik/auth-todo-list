import { Box } from '@mui/material';
import styled from 'styled-components';

export const EmptyDataStyled = styled(Box)`
  position: absolute;
  display: flex;
  flex-direction: column;
  width: 150px;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
